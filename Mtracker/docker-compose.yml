services:
  mtracker:
    build: .
    image: mtracker:latest
    container_name: mtracker
    ports:
      - "5000:5000"
    volumes:
      - mtracker-data:/data
    restart: unless-stopped

volumes:
  mtracker-data:
